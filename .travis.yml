language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-sails-angular' ]; then cd .. && eval "mv $currentfolder generator-sails-angular" && cd generator-sails-angular; fi

